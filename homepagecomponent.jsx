import React from 'react';

const HomePage = () => {
    return (
        <div className="home-page">
            <h2>Simulation Area</h2>
            {/* Add graph container or simulation area component */}
            <div className="graph-container">
                {/* Graph or simulation area */}
            </div>
        </div>
    );
};

export default HomePage;
